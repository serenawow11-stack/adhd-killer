# 界面设计约定

参考：https://impeccable.style/designing/

场景是执行任务的工具，不是宣传落地页。首要动作是看清当前小时目标、开始一个有目的的使用会话。

- 桌面：浅鼠尾草绿侧栏，中央行动区，右侧规划区；移动端顺序为当前目标、使用目的、清单、计划、足迹。
- 背景 #f7f8f5，文字 #273e37，行动色 #244e42；深绿仅强调当前目标，橙色只作极少点缀。
- 本地系统字体，清晰中文层级；时间采用等宽数字；没有网络字体与装饰图片依赖。
- 轻边框、16px 圆角、无重投影；列表用分隔线。减少同等权重的大卡片和重复说明。
- 真数据与空状态，不填充虚构任务、效率或成就。技术说明折叠在页尾，提醒限制仍可展开查看。
- 交互保留键盘焦点，尊重减少动态效果偏好，主要操作至少 44px。
- 保留原 localStorage key 与数据结构。PWA 静态缓存升级到 v2。
